const uint histogramBinCount = 256;

layout (std430, set = 3, binding = 1) buffer Histogram {
    uint histogram[];
};

float Luma(vec3 color) {

    const vec3 luma = vec3(0.299, 0.587, 0.114);
    return dot(color, luma);

}